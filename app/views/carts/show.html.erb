<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
<h1><%#= button_to "Continue Shoping" , stores_path, :method => "get",class: "btn btn-info btn-lg" %></h1>
 <%= button_to "<Continue Shoping" ,stores_path, :method => "get",class: "btn btn-success" %></td>
 <h3>Your Cart</h3>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
       <th>cart_Name</th>
      <th>Quantity</th>
      <th>Price</th> 
      <th>SubTotal</th>
    
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
<% @cart.lineitems.each do |item| %>
<tr>
 
<td><%= item.sticker.name %></td>

<td><%= item.quantity %>&times;</td>
<td class="item_price"><%= number_to_currency(item.unit_price) %></td>
<td colspan="2"><%= number_to_currency(item.total_price) %></td>
    
</tr>
    <td><%#= link_to 'Destroy', @lineitem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<% end %>
</td>
</tbody>
</table>
<tbody>
  <tr>
<div class="col-md-5 col-md-offset-4">
<div class="pull-right"><td><h1>Grand Total</td>

:<%= number_to_currency(@cart.total_price) %></td></div>

<td><%= link_to 'remove', cart_path(),class: "btn btn-danger" , method: :delete, data: { confirm: 'Are you sure?'} %></td>
  </tr></h1>
</tbody>
 </table>
</div>
</td></div></div></tr></tbody>
    
   
 
    </tr>

    </tbody>
    <div class="text-center">
    <td><%= button_to "Place Order>",new_order_path, :method => "get" ,class: "btn btn-warning" %>
 </div>
 <style>
   div.text-center{
     float: center;
   }
  </style>
    