<p id="notice"><%= notice %></p>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% if @cart.present? %>
    <% @carts.each do |cart| %>
      <tr>
        <td><%= link_to 'Show', cart %></td>
        <td><%= link_to 'Edit', edit_cart_path(cart) %></td>
        <td><%= link_to 'Destroy', cart, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      

    <% end %>
    <% end %>
  </tbody>
</table>

<br>
<h1><%= button_to "Continue Shoping" ,stores_path, :method => "get" %></h1>
<div class="img-left">

<%= image_tag("cart2.png", alt: "File Name Fancy", size: "50*80")%>
</div>
<%#= link_to 'New Cart', new_cart_path %>
<style>
   div.img-left{
     float: left;
   }
  </style>