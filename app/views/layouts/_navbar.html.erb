<div class="text-right">
<div class="icon-shopping" style="cursor: pointer;">
   <%= current_cart.lineitems.present? ? ("<span style='position: relative;left: -20px;'>#{current_cart.lineitems.count} Items in Your Cart</span>".html_safe) : ("<span style='position: relative;left: -15px;'>Your Cart Is Empty</span>".html_safe) %>
 </div>
 </div>

<%= form_tag stores_path, :id => 'search_form', :method => :get do %>
 <%= select_tag :category, options_for_select([["Stickers",""]]+Category.all.map{|a| a.name}),class: "btn btn-success"  %>
<% end %>

<script>
  $('#category').change(function()
 {
     $('#search_form').submit();
 });
</script>
<style>
   div.text-right{
     float: right;
}
  </style>
