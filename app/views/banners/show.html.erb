<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @banner.name %>
</p>
<p>
<% @banner.images.each do |i| %>
<%= image_tag(i.image.url, :height => 50, :width =>80 ) %>
<% end %>
</p>

<%= link_to 'Edit', edit_banner_path(@banner) %> 
<%= link_to 'Back', banners_path %>
