<p id="notice"><%= notice %></p>


<div class="text-right">
<%#= button_to "Back" , dashboards_index_path, :method => "get" %>
<h1><%= link_to '+New Banner', new_banner_path %></h1>
</div>
<style>
   div.text-right{
     float: right;
}
  </style>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
        <th>Banner</th>
      <th>Name</th>
  
      <th>Caption part</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @banners.each do |banner| %>
      <tr>    
       <td><% banner.images.each do |image| %>
<%= link_to  image_tag(image.image.url, :height => 50, :width =>80 ), banner_path(banner.id) %>
<% end %></td>
        <td><%= banner.name %></td>

        <td><%= link_to 'Show', banner %>
       <%= link_to 'Edit', edit_banner_path(banner) %>
       <%= link_to 'Destroy', banner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 <style>
   div.text-right{
     float: right;
}
  </style>


<%#= button_to "new banner", new_banner_path, :class => "button", :method => :get  %>
